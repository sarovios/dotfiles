# ~/.config/starship.toml
# Starship prompt configuration

format = """
$os$username$hostname$directory$git_branch$git_status$python$nodejs$rust$golang$package$memory_usage$cmd_duration
$character"""

# Allow a bit more time for slower commands (git status, node)
command_timeout = 2000

# Disable the blank line at the start of the prompt
add_newline = false

# OS module
[os]
format = '[$symbol](bold white) '
disabled = false

[os.symbols]
Alpaquita = "🔔"
Alpine = "🏔️"
Amazon = "🙂"
Android = "🤖"
Arch = "🎗️"
Artix = "🎗️"
CentOS = "💠"
Debian = "🌀"
DragonFly = "🐉"
Emscripten = "🔗"
EndeavourOS = "🚀"
Fedora = "🎩"
FreeBSD = "😈"
Garuda = "🦅"
Gentoo = "🗜️"
HardenedBSD = "🛡️"
Illumos = "🐦"
Linux = "🐧"
Mabox = "📦"
Macos = "🍎"
Manjaro = "🥭"
Mariner = "🌊"
MidnightBSD = "🌘"
Mint = "🌿"
NetBSD = "🚩"
NixOS = "❄️"
OpenBSD = "🐡"
OpenCloudOS = "☁️"
openEuler = "🦌"
openSUSE = "🦎"
OracleLinux = "🦴"
Pop = "🍭"
Raspbian = "🍓"
Redhat = "🎩"
RedHatEnterprise = "🎩"
Redox = "🧪"
Solus = "⛵"
SUSE = "🦎"
Ubuntu = "🎯"
Unknown = "❓"
Windows = "🪟"

# Username
[username]
style_user = 'white bold'
style_root = 'black bold'
format = '[$user]($style) '
disabled = false
show_always = true

# Directory
[directory]
truncation_length = 3
truncation_symbol = '…/'
home_symbol = '󰋜 ~'
read_only_style = '197'
read_only = '  '
format = 'at [$path]($style)[$read_only]($read_only_style) '

# Git
[git_branch]
symbol = ' '
format = 'on [$symbol$branch]($style)'
truncation_length = 4
truncation_symbol = '…/'
style = 'green'

[git_status]
format = '[\($all_status$ahead_behind\)]($style) '
style = 'green'
conflicted = '🏳'
up_to_date = ' '
untracked = ' '
ahead = '⇡${count}'
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
behind = '⇣${count}'
stashed = ' '
modified = ' '
staged = '[++\($count\)](green)'
renamed = '襁 '
deleted = ' '

# Languages
[python]
symbol = ' '
python_binary = ['./venv/bin/python', 'python', 'python3', 'python2']
format = 'via [${symbol}${pyenv_prefix}(${version} )(\($virtualenv\) )]($style)'
style = 'yellow'

[nodejs]
format = 'via [ $version](green) '
disabled = false

[rust]
format = 'via [ $version](red) '

[golang]
format = 'via [ $version](cyan) '

# Package version
[package]
format = 'is [ $version](208 bold) '

# Memory usage
[memory_usage]
disabled = false
threshold = -1
symbol = ' '
style = 'cyan'
format = 'via $symbol[$ram( | $swap)]($style) '

# Command duration
[cmd_duration]
format = 'took [$duration](yellow) '
min_time = 2_000

# Character
[character]
success_symbol = '[❯](purple)'
error_symbol = '[❯](red)'
vimcmd_symbol = '[❮](green)'
